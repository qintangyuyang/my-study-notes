# 委托与事件

> 摸底第 15 题你概念答了满分（委托可外部赋值/调用，事件外部只能订阅），但第 20 题让你**写**订阅代码却写不出来。
> 本周目标：把「懂概念」变成「写得出」——不看资料完成 EventDemo.cs。

## ① 概念总结（用自己的话回答 + 写代码验证）

- 委托（delegate）的本质是什么？（提示：它是「方法类型的变量」）
- 委托与事件的区别？为什么事件更安全？
- `+=`（订阅）和 `-=`（退订）分别做什么？为什么不能直接给事件用 `=`？
- `?.Invoke()` 是什么意思？（提示：判空再触发）
- Lambda `() => Console.WriteLine(...)` 和委托是什么关系？
- 你在公司/自己项目里哪里用过类似机制？（你写过事件管理器——对比一下和 event 的差别）

## ② 最小可运行示例

- [ ] 完成 `csharp/exercises/week02/EventDemo.cs`：声明事件 → 两个订阅者 → 触发 → 退订 → 再触发
- [ ] 实验：试试在外部直接调用事件或给事件赋值 `=`，看编译器报什么错（把错误信息抄下来——这正好说明事件为什么安全）

## ③ 踩坑记录

| 坑 | 原因 | 解决 |
|---|---|---|
|   |   |   |
